#!/bin/bash

# Call the VSCode theme setter with Cursor-specific parameters
shellos-theme-set-vscode cursor "$HOME/.config/Cursor/User/settings.json" "$HOME/.local/state/shellos/toggles/skip-cursor-theme-changes" Cursor
